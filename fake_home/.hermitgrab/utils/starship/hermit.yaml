provides:
  - starship

files:
  - source: starship.toml
    target: ~/.config/starship.toml
install:
  - name: starship
    check_cmd: starship --version
    source: cargo_binstall
    version: latest
sources:
  cargo_binstall: echo cargo binstall -y {{ name }}
