sources:
  apt: |
    #!/bin/bash
    echo apt-get update
    echo apt-get install -y {{ name }} {{ version }} {{ bla }}
  brew: brew install {{ name }}
profiles:
  karsten:
    - fish
    - fish-latest
    - starship